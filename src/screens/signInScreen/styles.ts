import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 20
  },

  logo: {
    width: '70%',
    maxWidth: 300,
    maxHeight: 200,
    resizeMode: 'contain'
  }
})

export default styles